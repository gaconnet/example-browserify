{
  "name": "example-browserify",
  "version": "0.0.0",
  "description": "try it out: browserify transforms and pipeline",
  "license": "MIT",
  "author": "CJ Gaconnet",
  "scripts": {
    "browserify": "browserify index.js -o bundle.js",
    "watchify": "watchify index.js -o bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "index.js",
  "browserify": {
      "transform": "es6ify"
  },
  "devDependencies": {
    "es6ify": "^1.4.0",
    "browserify": "^6.0.2",
    "watchify": "^2.0.0"
  }
}
